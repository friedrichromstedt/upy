0.2.3b 2/10
------

- changes in class Characteristic:
	In __init__(shape), removing parameter DTYPE.
	In some functions using .dtype as initialisation parameter for target
		Characteristics, no longer doing so.

0.2.2b 2/10
------

- changes in class Characteristic:
	In get_dispersion(), BUGFIX: always returning an array of shape .shape.

0.2.1b 2/10
------

- general changes:
	Allowing for addition on a key.

- changes in class Characteristic:
	add(other, key), code taken from __iadd__() with additional argument.
	__setitem__(), implementing the thing.
	
0.2.0b 2/10
------

- general changes:
	Fully reworking the module towards a new storage scheme.

- class Characteristic:
	__init__(shape)
	append(dependency)
	get_dispersion()
	__iadd__(other)
	__mul__(other)
	__rmul__(other)
	__getitem__((shape, key))
	__setitem__(key, value)
	__len__()
	flatten(new_shape, *, **)
	repeat(new_shape, *, **)
	reshape(new_shape, *, **)
	broadcast(shape)
	broadcasted(shape)

0.1.0b 2/10
------

- taking definitions over from upy.core 0.2.5b.
