0.1.1b 2/10
------

- general changes:
	Adding the workaround for the numpy calling convention also the the
		equal() wrapping class.

- in class Equal:
	__call__(a, b, *args, **kwargs)

0.1.0b 2/10
------

- taking definitions over from upy.core 0.2.5b.
